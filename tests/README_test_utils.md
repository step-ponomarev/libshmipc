# Test Utilities

## 📋 Описание
Утилиты для тестирования `libshmipc` - предоставляют вспомогательные классы, функции и макросы для упрощения написания тестов и обеспечения их надежности.

## 🎯 Цель утилит
Упростить написание тестов, обеспечить единообразие проверок, автоматизировать управление ресурсами и предоставить удобные абстракции для тестирования.

## 🔧 Основные компоненты

### **1. Wrapper классы**
- `BufferWrapper` - автоматическое управление IPC буферами
- `ChannelWrapper` - автоматическое управление IPC каналами
- `EntryWrapper` - автоматическое управление IPC записями

### **2. Проверочные макросы**
- `CHECK_OK()` - проверка успешных результатов
- `CHECK_ERROR()` - проверка ошибок
- `CHECK_ERROR_WITH_FIELDS()` - проверка ошибок с полями

### **3. Вспомогательные функции**
- `write_data()` - запись данных в буфер/канал
- `read_data()` - чтение данных из буфера/канала
- `fill_buffer()` - заполнение буфера данными

## 🧪 Wrapper классы

### **BufferWrapper**
```cpp
class BufferWrapper {
public:
    explicit BufferWrapper(size_t size);
    ~BufferWrapper();
    IpcBuffer* get() const;
    IpcBuffer* operator->() const;
    // Move semantics
    BufferWrapper(BufferWrapper&& other) noexcept;
    BufferWrapper& operator=(BufferWrapper&& other) noexcept;
};
```
**Что делает:** Автоматически создает и уничтожает IPC буферы
**Как работает:** RAII - автоматическое управление ресурсами
**Зачем:** Упростить тестирование и избежать утечек памяти

### **ChannelWrapper**
```cpp
class ChannelWrapper {
public:
    explicit ChannelWrapper(size_t size, const IpcChannelConfiguration& config = DEFAULT_CONFIG);
    ~ChannelWrapper();
    IpcChannel* get() const;
    IpcChannel* operator->() const;
    const uint8_t* get_mem() const;
    // Move semantics
    ChannelWrapper(ChannelWrapper&& other) noexcept;
    ChannelWrapper& operator=(ChannelWrapper&& other) noexcept;
};
```
**Что делает:** Автоматически создает и уничтожает IPC каналы
**Как работает:** RAII с поддержкой конфигурации каналов
**Зачем:** Упростить тестирование каналов и обеспечить правильную очистку

### **EntryWrapper**
```cpp
class EntryWrapper {
public:
    explicit EntryWrapper(size_t size);
    ~EntryWrapper();
    IpcEntry get() const;
    // Move semantics
    EntryWrapper(EntryWrapper&& other) noexcept;
    EntryWrapper& operator=(EntryWrapper&& other) noexcept;
};
```
**Что делает:** Автоматически управляет IPC записями
**Как работает:** RAII для записей с автоматическим освобождением памяти
**Зачем:** Упростить работу с записями и избежать утечек памяти

## 🔍 Проверочные макросы

### **CHECK_OK**
```cpp
template<typename T>
void CHECK_OK(const T& result);
```
**Что делает:** Проверяет, что результат операции успешен
**Как работает:** Использует `is_ok()` методы для различных типов результатов
**Зачем:** Упростить проверку успешных операций

### **CHECK_ERROR**
```cpp
template<typename T>
void CHECK_ERROR(const T& result, IpcStatus expected_status);
```
**Что делает:** Проверяет, что результат операции содержит ошибку
**Как работает:** Проверяет статус ошибки и сравнивает с ожидаемым
**Зачем:** Упростить проверку ошибок

### **CHECK_ERROR_WITH_FIELDS**
```cpp
template<typename T>
void CHECK_ERROR_WITH_FIELDS(const T& result, IpcStatus expected_status, 
                            uint64_t expected_field1, uint64_t expected_field2);
```
**Что делает:** Проверяет ошибку с дополнительными полями
**Как работает:** Проверяет статус и поля структуры ошибки
**Зачем:** Упростить проверку детальной информации об ошибках

## 🚀 Вспомогательные функции

### **write_data**
```cpp
template<typename T>
IpcBufferWriteResult write_data(IpcBuffer* buffer, const T& data);

template<typename T>
IpcChannelWriteResult write_data(IpcChannel* channel, const T& data);
```
**Что делает:** Записывает данные в буфер или канал
**Как работает:** Шаблонная функция для различных типов данных
**Зачем:** Упростить запись данных в тестах

### **read_data**
```cpp
template<typename T>
T read_data(IpcBuffer* buffer);

template<typename T>
T read_data(IpcChannel* channel);
```
**Что делает:** Читает данные из буфера или канала
**Как работает:** Шаблонная функция с автоматическим управлением памятью
**Зачем:** Упростить чтение данных в тестах

### **fill_buffer**
```cpp
void fill_buffer(IpcBuffer* buffer, size_t count);
```
**Что делает:** Заполняет буфер тестовыми данными
**Как работает:** Записывает последовательные числа в буфер
**Зачем:** Упростить подготовку тестовых данных

## 📊 Константы

### **Размеры буферов**
```cpp
constexpr size_t SMALL_BUFFER_SIZE = 128;
constexpr size_t MEDIUM_BUFFER_SIZE = 256;
constexpr size_t LARGE_BUFFER_SIZE = 1024;
```
**Что делает:** Предоставляет стандартные размеры для тестов
**Как работает:** Константы для различных сценариев тестирования
**Зачем:** Упростить выбор размеров для тестов

### **Конфигурация каналов**
```cpp
const IpcChannelConfiguration DEFAULT_CONFIG = {
    .max_round_trips = 1024, 
    .start_sleep_ns = 1000, 
    .max_sleep_ns = 100000
};
```
**Что делает:** Предоставляет стандартную конфигурацию для каналов
**Как работает:** Конфигурация по умолчанию для тестов
**Зачем:** Упростить создание каналов в тестах

## 🎯 Примеры использования

### **Базовое использование BufferWrapper**
```cpp
TEST_CASE("basic buffer test") {
    test_utils::BufferWrapper buffer(1024);
    
    // Автоматическое управление ресурсами
    int data = 42;
    test_utils::write_data(buffer.get(), data);
    
    int read_data = test_utils::read_data<int>(buffer.get());
    CHECK(read_data == data);
    
    // Автоматическое освобождение при выходе из scope
}
```

### **Использование ChannelWrapper**
```cpp
TEST_CASE("basic channel test") {
    test_utils::ChannelWrapper channel(1024);
    
    // Автоматическое управление каналом
    int data = 42;
    test_utils::write_data(channel.get(), data);
    
    int read_data = test_utils::read_data<int>(channel.get());
    CHECK(read_data == data);
    
    // Автоматическое освобождение
}
```

### **Проверка ошибок**
```cpp
TEST_CASE("error handling test") {
    test_utils::BufferWrapper buffer(1024);
    
    // Проверка успешной операции
    int data = 42;
    auto write_result = test_utils::write_data(buffer.get(), data);
    test_utils::CHECK_OK(write_result);
    
    // Проверка ошибки
    auto error_result = ipc_buffer_write(nullptr, &data, sizeof(data));
    test_utils::CHECK_ERROR(error_result, IPC_ERR_INVALID_ARGUMENT);
}
```

### **Продвинутое использование**
```cpp
TEST_CASE("advanced test") {
    test_utils::BufferWrapper buffer(test_utils::LARGE_BUFFER_SIZE);
    
    // Заполнение буфера данными
    test_utils::fill_buffer(buffer.get(), 100);
    
    // Проверка данных
    for (int i = 0; i < 100; ++i) {
        int data = test_utils::read_data<int>(buffer.get());
        CHECK(data == i);
    }
}
```

## 🔧 Преимущества утилит

### **1. Упрощение тестов**
- Автоматическое управление ресурсами
- Упрощенные проверки
- Меньше boilerplate кода

### **2. Надежность**
- RAII предотвращает утечки памяти
- Единообразные проверки
- Автоматическая очистка ресурсов

### **3. Читаемость**
- Понятные имена функций
- Шаблонные функции для типов
- Консистентный API

### **4. Поддержка**
- Легко добавлять новые функции
- Расширяемость для новых типов
- Обратная совместимость

## 📈 Результаты
- **Упрощение тестов** - меньше кода, больше функциональности
- **Повышение надежности** - автоматическое управление ресурсами
- **Улучшение читаемости** - понятные имена и структура
- **Ускорение разработки** - готовые компоненты для тестов
