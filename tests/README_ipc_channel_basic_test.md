# IPC Channel Basic Tests

## 📋 Описание
Базовые тесты для `ipc_channel.h` - проверяют основную функциональность IPC каналов, включая создание, подключение, чтение, запись и управление данными с поддержкой таймаутов и retry механизмов.

## 🎯 Цель тестов
Убедиться, что все основные функции IPC каналов работают корректно в нормальных условиях и правильно обрабатывают ошибки, включая специфичные для каналов функции (таймауты, retry).

## 📊 Количество тестов
**14 тестов** - базовое покрытие API каналов

## 🔧 Тестируемые функции

### **Создание и подключение каналов**
- `ipc_channel_create()` - создание канала с конфигурацией
- `ipc_channel_connect()` - подключение к существующему каналу
- `ipc_channel_destroy()` - уничтожение канала

### **Операции с данными**
- `ipc_channel_write()` - запись данных
- `ipc_channel_read()` - чтение данных (с retry)
- `ipc_channel_try_read()` - попытка чтения без ожидания
- `ipc_channel_read_with_timeout()` - чтение с таймаутом

### **Просмотр и навигация**
- `ipc_channel_peek()` - просмотр без чтения
- `ipc_channel_skip()` - пропуск записей
- `ipc_channel_skip_force()` - принудительный пропуск

## 🧪 Типы тестов

### **1. Конфигурация каналов**
```cpp
TEST_CASE("invalid config")
```
**Что проверяет:** Валидацию конфигурации каналов
**Как проверяет:** 
- Создает каналы с неверными параметрами конфигурации
- Проверяет, что API возвращает соответствующие ошибки
- Проверяет поля структур ошибок
**Зачем:** Убедиться, что конфигурация валидируется правильно

### **2. Операции записи**
```cpp
TEST_CASE("write too large entry")
TEST_CASE("write read")
```
**Что проверяет:** Запись данных в канал
**Как проверяет:**
- Пытается записать данные разного размера
- Проверяет обработку слишком больших записей
- Проверяет успешную запись и чтение
**Зачем:** Убедиться, что данные корректно сохраняются в канале

### **3. Операции чтения**
```cpp
TEST_CASE("write try read")
TEST_CASE("try read empty")
TEST_CASE("read retry limit reached")
```
**Что проверяет:** Различные способы чтения из канала
**Как проверяет:**
- Тестирует `try_read` - немедленное чтение без ожидания
- Тестирует чтение из пустого канала
- Тестирует достижение лимита retry попыток
**Зачем:** Убедиться, что различные режимы чтения работают правильно

### **4. Таймауты**
```cpp
TEST_CASE("read timeout")
```
**Что проверяет:** Чтение с таймаутом
**Как проверяет:**
- Создает канал без данных
- Пытается читать с коротким таймаутом
- Проверяет, что операция завершается по таймауту
**Зачем:** Убедиться, что таймауты работают корректно

### **5. Просмотр и навигация**
```cpp
TEST_CASE("peek")
TEST_CASE("peek empty")
TEST_CASE("skip corrupted entry")
TEST_CASE("skip force")
```
**Что проверяет:** Просмотр и навигацию по каналу
**Как проверяет:**
- Тестирует просмотр данных без чтения
- Тестирует просмотр пустого канала
- Тестирует пропуск поврежденных записей
- Тестирует принудительный пропуск
**Зачем:** Убедиться, что навигация по каналу работает правильно

### **6. Специфичные сценарии каналов**
```cpp
TEST_CASE("channel read before commit via channel")
TEST_CASE("channel double commit")
```
**Что проверяет:** Специфичные для каналов сценарии
**Как проверяет:**
- Тестирует чтение до коммита через канал
- Тестирует двойной коммит
- Проверяет корректность состояний канала
**Зачем:** Убедиться, что каналы правильно управляют состоянием

## 🚀 Примеры использования

### **Базовое использование канала**
```cpp
// Создание канала с конфигурацией
IpcChannelConfiguration config = {
    .max_round_trips = 1024,
    .start_sleep_ns = 1000,
    .max_sleep_ns = 100000
};

uint8_t mem[1024];
IpcChannelResult result = ipc_channel_create(mem, 1024, config);
CHECK(IpcChannelResult_is_ok(result));

// Запись данных
int data = 42;
IpcChannelWriteResult write_result = ipc_channel_write(result.result, &data, sizeof(data));
CHECK(IpcChannelWriteResult_is_ok(write_result));

// Чтение данных
IpcEntry entry;
IpcChannelReadResult read_result = ipc_channel_read(result.result, &entry);
CHECK(IpcChannelReadResult_is_ok(read_result));
```

### **Чтение с таймаутом**
```cpp
// Чтение с таймаутом
struct timespec timeout = {0, 1000000}; // 1ms
IpcChannelReadWithTimeoutResult timeout_result = 
    ipc_channel_read_with_timeout(channel, &entry, &timeout);

if (timeout_result.ipc_status == IPC_ERR_TIMEOUT) {
    // Обработка таймаута
} else if (timeout_result.ipc_status == IPC_OK) {
    // Данные получены
}
```

### **Попытка чтения без ожидания**
```cpp
// Попытка чтения без ожидания
IpcChannelTryReadResult try_result = ipc_channel_try_read(channel, &entry);
if (try_result.ipc_status == IPC_OK) {
    // Данные доступны
} else if (try_result.ipc_status == IPC_EMPTY) {
    // Канал пуст
}
```

## ⚡ Особенности каналов

### **1. Retry механизм**
- Каналы автоматически повторяют операции при конфликтах
- Конфигурируемое количество попыток
- Экспоненциальная задержка между попытками

### **2. Таймауты**
- Поддержка чтения с таймаутом
- Настраиваемые параметры ожидания
- Graceful timeout handling

### **3. Состояние канала**
- Каналы отслеживают состояние операций
- Поддержка commit/rollback операций
- Защита от race conditions

## 🔍 Ключевые проверки

### **1. Конфигурация**
- Валидация параметров конфигурации
- Корректная обработка неверных значений
- Значения по умолчанию

### **2. Операции с данными**
- Корректность записи и чтения
- Обработка больших данных
- Граничные случаи

### **3. Таймауты и retry**
- Корректность таймаутов
- Работа retry механизма
- Обработка лимитов

### **4. Состояние канала**
- Корректность переходов состояний
- Защита от некорректных операций
- Управление ресурсами

## 📈 Результаты
- **14 тестов** - все проходят
- **50 assertions** - все проверки успешны
- **Базовое покрытие** API каналов
- **Надежная основа** для продвинутых тестов

## 🎯 Отличия от буферов
Каналы предоставляют дополнительные возможности:
- **Retry механизм** - автоматические повторы при конфликтах
- **Таймауты** - контроль времени выполнения операций
- **Состояние** - отслеживание состояния операций
- **Конфигурация** - настройка поведения канала
