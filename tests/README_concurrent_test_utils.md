# Concurrent Test Utilities

## 📋 Описание
Утилиты для тестирования многопоточности `libshmipc` - предоставляют вспомогательные функции и шаблоны для тестирования concurrent операций с IPC буферами и каналами.

## 🎯 Цель утилит
Упростить написание многопоточных тестов, обеспечить единообразие concurrent операций, автоматизировать управление потоками и предоставить удобные абстракции для тестирования race conditions.

## 🔧 Основные компоненты

### **1. Concurrent операции**
- `concurrent_write()` - конкурентная запись в буфер
- `concurrent_read()` - конкурентное чтение из буфера
- `concurrent_peek()` - конкурентный просмотр буфера

### **2. Race condition тесты**
- `test_race_between_skip_and_read_buffer()` - гонка между пропуском и чтением
- `test_race_between_skip_and_read_channel()` - гонка в каналах

### **3. Вспомогательные функции**
- `wait_for_expected_size()` - ожидание определенного размера
- `wait_for_expected_size_channel()` - ожидание в каналах
- `verify_data_integrity()` - проверка целостности данных

## 🧪 Concurrent операции

### **concurrent_write**
```cpp
template<typename BufferType>
void concurrent_write(BufferType* buffer, size_t from, size_t to, 
                     concurrent_set<size_t>* dest);
```
**Что делает:** Выполняет конкурентную запись в буфер
**Как работает:** 
- Записывает данные от `from` до `to` в буфер
- Добавляет успешно записанные данные в `dest`
- Обрабатывает ошибки записи
**Зачем:** Тестировать конкурентную запись и обработку ошибок

### **concurrent_read**
```cpp
template<typename BufferType>
void concurrent_read(BufferType* buffer, size_t expected, 
                   concurrent_set<size_t>* dest);
```
**Что делает:** Выполняет конкурентное чтение из буфера
**Как работает:**
- Читает данные из буфера до достижения `expected` размера
- Добавляет прочитанные данные в `dest`
- Обрабатывает ошибки чтения
**Зачем:** Тестировать конкурентное чтение и обработку ошибок

### **concurrent_peek**
```cpp
template<typename BufferType>
void concurrent_peek(BufferType* buffer, size_t expected, 
                   concurrent_set<size_t>* dest);
```
**Что делает:** Выполняет конкурентный просмотр буфера
**Как работает:**
- Просматривает данные в буфере до достижения `expected` размера
- Добавляет просмотренные данные в `dest`
- Обрабатывает ошибки просмотра
**Зачем:** Тестировать конкурентный просмотр и обработку ошибок

## 🔍 Race condition тесты

### **test_race_between_skip_and_read_buffer**
```cpp
void test_race_between_skip_and_read_buffer();
```
**Что делает:** Тестирует гонку между пропуском и чтением в буфере
**Как работает:**
- Создает буфер с тестовыми данными
- Запускает потоки для пропуска и чтения
- Проверяет отсутствие race conditions
**Зачем:** Убедиться, что буфер безопасен для concurrent операций

### **test_race_between_skip_and_read_channel**
```cpp
void test_race_between_skip_and_read_channel();
```
**Что делает:** Тестирует гонку между пропуском и чтением в канале
**Как работает:**
- Создает канал с тестовыми данными
- Запускает потоки для пропуска и чтения
- Проверяет отсутствие race conditions
**Зачем:** Убедиться, что канал безопасен для concurrent операций

## 🚀 Вспомогательные функции

### **wait_for_expected_size**
```cpp
template<typename BufferType>
void wait_for_expected_size(BufferType* buffer, size_t expected, 
                           concurrent_set<size_t>* dest);
```
**Что делает:** Ожидает достижения определенного размера в буфере
**Как работает:**
- Проверяет размер буфера
- Ждет достижения `expected` размера
- Добавляет данные в `dest` по мере поступления
**Зачем:** Синхронизировать потоки и обеспечить корректное тестирование

### **wait_for_expected_size_channel**
```cpp
template<typename ChannelType>
void wait_for_expected_size_channel(ChannelType* channel, size_t expected, 
                                   concurrent_set<size_t>* dest);
```
**Что делает:** Ожидает определенного размера в канале
**Как работает:**
- Проверяет размер канала
- Ждет достижения `expected` размера
- Добавляет данные в `dest` по мере поступления
**Зачем:** Синхронизировать потоки в каналах

### **verify_data_integrity**
```cpp
template<typename BufferType>
void verify_data_integrity(BufferType* buffer, size_t total, 
                          concurrent_set<size_t>* dest);
```
**Что делает:** Проверяет целостность данных в буфере
**Как работает:**
- Проверяет, что все данные от 0 до `total` присутствуют
- Проверяет отсутствие дубликатов
- Проверяет отсутствие пропусков
**Зачем:** Убедиться, что данные не потерялись и не исказились

## 🎯 Примеры использования

### **Базовый concurrent тест**
```cpp
TEST_CASE("concurrent write test") {
    test_utils::BufferWrapper buffer(1024);
    concurrent_set<size_t> written_data;
    
    // Запуск concurrent записи
    std::thread writer1([&] {
        concurrent_test_utils::concurrent_write(buffer.get(), 0, 100, &written_data);
    });
    
    std::thread writer2([&] {
        concurrent_test_utils::concurrent_write(buffer.get(), 100, 200, &written_data);
    });
    
    writer1.join();
    writer2.join();
    
    // Проверка целостности данных
    concurrent_test_utils::verify_data_integrity(buffer.get(), 200, &written_data);
}
```

### **Race condition тест**
```cpp
TEST_CASE("race condition test") {
    // Тест гонки в буфере
    concurrent_test_utils::test_race_between_skip_and_read_buffer();
    
    // Тест гонки в канале
    concurrent_test_utils::test_race_between_skip_and_read_channel();
}
```

### **Продвинутый concurrent тест**
```cpp
TEST_CASE("advanced concurrent test") {
    test_utils::BufferWrapper buffer(1024);
    concurrent_set<size_t> written_data;
    concurrent_set<size_t> read_data;
    
    // Запуск писателей
    std::vector<std::thread> writers;
    for (int i = 0; i < 4; ++i) {
        writers.emplace_back([&, i] {
            concurrent_test_utils::concurrent_write(buffer.get(), i * 100, (i + 1) * 100, &written_data);
        });
    }
    
    // Запуск читателей
    std::vector<std::thread> readers;
    for (int i = 0; i < 2; ++i) {
        readers.emplace_back([&] {
            concurrent_test_utils::concurrent_read(buffer.get(), 400, &read_data);
        });
    }
    
    // Ожидание завершения
    for (auto& writer : writers) writer.join();
    for (auto& reader : readers) reader.join();
    
    // Проверка результатов
    concurrent_test_utils::verify_data_integrity(buffer.get(), 400, &written_data);
    concurrent_test_utils::verify_data_integrity(buffer.get(), 400, &read_data);
}
```

## 🔧 Преимущества утилит

### **1. Упрощение concurrent тестов**
- Готовые функции для concurrent операций
- Автоматическое управление потоками
- Упрощенные проверки race conditions

### **2. Надежность**
- Проверка целостности данных
- Обработка ошибок в concurrent сценариях
- Автоматическая синхронизация

### **3. Читаемость**
- Понятные имена функций
- Шаблонные функции для различных типов
- Консистентный API

### **4. Поддержка**
- Легко добавлять новые concurrent операции
- Расширяемость для новых типов
- Обратная совместимость

## 📈 Результаты
- **Упрощение concurrent тестов** - меньше кода, больше функциональности
- **Повышение надежности** - автоматическая проверка race conditions
- **Улучшение читаемости** - понятные имена и структура
- **Ускорение разработки** - готовые компоненты для concurrent тестов

## 🎯 Практическое применение
Эти утилиты помогают:
- **Тестировать многопоточность** - проверять thread safety
- **Выявлять race conditions** - находить проблемы синхронизации
- **Проверять целостность данных** - убеждаться в корректности
- **Оптимизировать производительность** - находить узкие места
